





## influxdb:

```
influx --username admin --password influxdb


influx --username root --password taosdata

use mydb;

insert scada_value,id=1,station="bj",type="car" value=1.1,quality=10,flags=1

insert scada_value,id=1,station="bj",type="car" quality=10,flags=1

insert scada_value,id=2,station="sz",type="horse" value=3.1,quality=11,flags=1

insert scada_value,id=2,station="sh",type="plane" value=3.1,quality=11,flags=1

insert scada_value,id=3,station="sh",type="plane" value=3.1,quality=11,flags=1

insert scada_value,id=1,station=bj,type=car\  quality=10,flags=1


# 这一条执行失败，要转义 ,
insert scada_value,id=3,station="x,j",type="car" value=7.7,quality=11,flags=7
# 插入, 要转移 
insert scada_value,id=3,station="y\,n",type="car" value=7.7,quality=11,flags=7
```



## opentsdb





## mysql:

```
mysql -uroot -ptaosdata
create database zgc;
use zgc;


CREATE TABLE t1 (
   id INT AUTO_INCREMENT PRIMARY KEY,
   voltage int not null,
   v_blob blob not null,
   groupid int not null,
   location varchar(24) not null,
   time datetime DEFAULT CURRENT_TIMESTAMP
);

INSERT INTO t1 (voltage, v_blob, groupid, location) VALUES (11.1, '98f463', 1, "BJ");
INSERT INTO t1 (voltage, v_blob, groupid, location) VALUES (3.1, '98f463', 2, "BJ");
INSERT INTO t1 (voltage, v_blob, groupid, location) VALUES (3.3, '98f463', 1, "BJ");
INSERT INTO t1 (voltage, v_blob, groupid, location) VALUES (3.5, '98f463', 0, "BJ");
INSERT INTO t1 (voltage, v_blob, groupid, location) VALUES (4.5, '98f463', 0, "BJ");
INSERT INTO t1 (voltage, v_blob, groupid, location) VALUES (4.2, '98f463', 1, "BJ");



INSERT INTO t1 (voltage, v_blob, groupid, location) VALUES (3.7, '98f463', 2, "SH");


```

explorer 上的 sql 模板：

```
select distinct groupid, location from t1;

SELECT * FROM t1 WHERE time >= ${start_no_tz} AND time < ${end_no_tz} and ${groupid} and ${location} ORDER BY time;
```



```
select distinct groupid, location from t0
SELECT * FROM t0 WHERE time >= ${start_no_tz} AND time < ${end_no_tz} and ${groupid} and ${location} ORDER BY time
```



## mssql



```
```





## oracle





