

这样就支持 blob 数据类型：

```
{.name = "parser", .bytes = TSDB_XNODE_TASK_PARSER_LEN + VARSTR_HEADER_SIZE, .type = TSDB_DATA_TYPE_BLOB, .sysInfo = false},
改为：
{.name = "parser", .bytes = TSDB_XNODE_TASK_PARSER_LEN + BLOBSTR_HEADER_SIZE, .type = TSDB_DATA_TYPE_BLOB, .sysInfo = false},
```

兼容：

mndRetrieveXnodeTasks 函数里需要修改为, 

```
// parser
if (pObj->parserLen > 0 && pObj->parser != NULL) {
       buf[0] = 0;
     STR_WITH_MAXSIZE_TO_VARSTR(buf, pObj->parser, pShow->pMeta->pSchemas[cols].bytes);
     char *lastPos =
         stpncpy(blobDataVal(buf), (pObj->parser), (pShow->pMeta->pSchemas[cols].bytes) - BLOBSTR_HEADER_SIZE);
     blobDataSetLen(buf, (lastPos - (buf)-BLOBSTR_HEADER_SIZE));
     pColInfo = taosArrayGet(pBlock->pDataBlock, cols++);

...
}
```





后续 systable.c 里的表，还需要设计添加 version 字段！ 否则容易引起兼容性问题 ! !



​	







